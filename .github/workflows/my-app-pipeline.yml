name: CI

on:
  push:
    branches: ["main", "andre_dev"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Get Repo
        uses: actions/checkout@v4
      - name: Run a one-line script
        run: |
          echo Hello, world!
          dir
