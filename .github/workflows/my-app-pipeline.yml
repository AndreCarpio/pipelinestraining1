name: CI

on:
  push:
    branches: ["main", "andre_dev"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Get Repo
        uses: actions/checkout@v4
      - name: Run a one-line script
        run: |
          echo Hello, world!
          dir
  test:
    runs-on: self-hosted
    steps:
      - name: first step test
        run: echo "Runing test"
  publish:
    runs-on: self-hosted
    steps:
      - name: first step pusblish
        run: echo "Runing pusblish"
  deploy:
    runs-on: self-hosted
    steps:
      - name: first step deploy
        run: echo "Runing deploy"
